<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.graficarpixeles">

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GraficarPixeles">
        <activity android:name=".SQLActivity"></activity>
        <activity android:name=".ReproduccionAudio" />
        <activity android:name=".Circulo" />
        <activity android:name=".FondoyLineas" />
        <activity android:name=".GraficarPixeles" />
        <activity android:name=".MediaRecorder" />
        <activity android:name=".MenuProgramas" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>